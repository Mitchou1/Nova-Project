# --- Dépendances spécifiques Raspberry Pi ---
# Installées automatiquement sur la Pi par start_nova.sh.

# rpi-lgpio remplace RPi.GPIO avec la meme API (import RPi.GPIO) : l'ancien
# RPi.GPIO ne fonctionne pas sur Pi 5 (puce GPIO RP1).
rpi-lgpio>=0.6
smbus2>=0.4.2
spidev>=3.6
pyserial>=3.5
gpsd-py3>=0.3.0

# Whisper et Piper sont installés par scripts/setup_ai.sh
# (paquets lourds, compilation longue sur Pi Zero 2 W)
