# --- Dépendances spécifiques Raspberry Pi ---
# Installées uniquement sur le Pi : pip install -r requirements-pi.txt

RPi.GPIO==0.7.1
smbus2==0.4.2
spidev==3.6
pyserial==3.5
gpsd-py3==0.3.0

# Whisper et Piper sont installés par scripts/setup_ai.sh
# (paquets lourds, compilation longue sur Pi Zero 2 W)
