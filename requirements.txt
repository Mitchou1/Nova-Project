# Versions minimales (>=) et non figees (==) : les anciennes versions figees
# ne s'installaient plus sous Python 3.13 (PC et Pi), et pip garde ainsi tel
# quel tout paquet deja installe et compatible.

# Interface graphique
kivy[base]>=2.3.1

# IA embarquee (STT, LLM, TTS) — hors ligne
llama-cpp-python>=0.3.34
faster-whisper>=1.2.1
piper-tts>=1.6.0
sounddevice>=0.5.1
numpy>=2.1

# GPS
gps>=3.19

# Utilitaires
requests>=2.31.0
Pillow>=10.4.0

# Tests
pytest>=8.0.0

# Capteurs & GPIO : voir requirements-pi.txt (installe seulement sur la Pi
# par start_nova.sh)
